import styled from 'styled-components'

export const SeePass = styled.div`
    display: flex;
    justify-content: flex-end;
    align-items: center;

    position: absolute;
    right: 0;
    svg {
        color: var(--color-secundary);
    }
`