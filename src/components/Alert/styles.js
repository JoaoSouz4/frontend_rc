import styled from 'styled-components';

export const AlertContainer = styled.div`
    display: flex;
    align-items: center;
    background: var(--color-black);
    color: white;
    padding: 1.5rem;
    box-shadow: 1px 1px 7px 3px rgba(0, 0, 0, 0.407);
    font-size: 1rem;
    gap: 1rem;
` 